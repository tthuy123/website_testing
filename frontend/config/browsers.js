// Cấu hình trình duyệt mặc định cho newContext()
export const browserConfig = {
  viewport: { width: 1366, height: 768 },
  // headless: false, // mở UI khi debug
  userAgent: 'k6-browser-pom/1.0',
};
